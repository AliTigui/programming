/*
for make regex in kotlin we use Regex("here regular expression") for work with it it better store it in variable
val exp = Regex("") for chaeck if string match regex we use the method matches in the regex variable exemple exp.matches(str) it return true or false

*/
